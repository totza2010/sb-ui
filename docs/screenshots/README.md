# README screenshots

Drop PNGs here with these exact names (referenced from the top-level `README.md`):

| File | Shot |
|---|---|
| `dashboard.png` | **Hero** — full Dashboard with the live Storage flow diagram (the cloud ↔ mounts ↔ unionfs ↔ apps pipeline). |
| `app-manager.png` | App Manager — installed apps grouped by category. |
| `container-detail.png` | Docker page with a container **detail drawer** open (stats + inspect + logs). |

Optional extras you can add and link in the README later:

| File | Shot |
|---|---|
| `docker.png` | Docker page — container table with live CPU/MEM. |
| `setup-wizard.png` | First-run Setup Wizard (install type step). |
| `connection-setup.png` | Connection Setup (local / SSH). |

Tips:
- Capture at a consistent width (the app is widescreen — ~1900px looks good).
- PNG, light theme, trim browser chrome.
- Keep file sizes reasonable (run them through an optimizer if large).
